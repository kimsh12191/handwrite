# 금융권 손글씨 인식 프로젝트 — 계획 (v4)

## 목표

금융권 양식의 손글씨를 인식한다. 실제 라벨 데이터가 부족하므로 **합성 데이터로 학습**한다.
합성기는 **필기 사진 한 장**에서 그 사람 필체를 얻고, **사람만큼 매번 다르게** 쓴다.

```
필기 사진 1장 → 자모별 골격 + 변동 구조 → 임의 텍스트를 그 사람 필체로 생성
```

**"매번 다르게"가 요구사항이다.** 폰트·폰트생성형(GAN)의 실패는 변동이 작은 게 아니라
**0** 이라는 것 — 같은 '가' 가 항상 같은 픽셀이라 즉시 티가 난다.

---

## 원칙

**컴포넌트를 쌓는 순서가 아니라, 답이 나오면 다음 결정이 바뀌는 질문 순서로 진행한다.**

v1·v2·v3 에서 세 번 같은 원인으로 실패했다 — 핵심 질문 주변에 인프라를 짓고
핵심 질문은 건드리지 않았다.
- v1: 시뮬레이터를 한 번도 실행하지 않고 문서부터 씀 (글자를 못 그리는 코드였음)
- v2: 합성 데이터로 분류기를 학습해 MNIST 정확도를 잼 (분포 커버리지 질문, 원래 Q4)
- v2': 파라미터 스윕 (내가 짠 코드가 짠 대로 도는지 확인. 구조적으로 참)
- v3: 목적함수를 검증하지 않은 채 그 위에서 LLM·수치·이체를 비교함

**추가 원칙 — 심판을 먼저 검증한다.** v3 이 남긴 교훈이다. 지표가 틀리면
그 위의 모든 비교가 무의미하다.

---

## v3 를 폐기하는 근거 (전부 측정된 것)

v3 의 전제는 **"전역 스타일 파라미터 13개를 LLM 이 조종한다"** 였다. 셋이 무너졌다.

| # | 측정 | 출처 |
|---|---|---|
| 1 | **골격에 도달 못 함.** 이체 메뉴에 그 사람 형태가 없으면 끝. 실제 필기 `0`→0.57, `7`→0.73 (`1` 은 0.14~0.27 로 양호) | `49c4242` |
| 2 | **LLM 이 연속 파라미터 조종에서 짐.** LLM 2라운드 0.661 vs 수치 탐색 0.402 | `98143be` |
| 3 | **목적함수가 시각 유사도와 반대로 움직임.** 실제보다 2배 굵은 획(0.218)이 거리 0.569 로, 실제에 가까운 굵기(0.090, 거리 0.629)를 이김 | `4a00c05` |
| 4 | 전역 파라미터 피팅은 전이가 안 됨 — 일관된 한 사람은 나오나 **타겟을 닮지 않음** | `98143be` Q2 |

1·4 의 원인은 같다: **골격이 손으로 적은 이산 메뉴(이체 34개)라 만들 수가 없다.**
3 의 원인 하나는 해상도다 — 28×28 에서 획 굵기 2~3px 에 blur 를 먹이면 형태 정보가
거의 남지 않아 겹침 면적이 지표를 지배한다.

---

## 새 구조

### 무엇을 바꾸나

| | v3 | v4 |
|---|---|---|
| 단위 | 통글자 | **획 / 자모** |
| 골격 | 이체 메뉴 34개 (이산, 손으로 작성) | 획 파라미터 (연속, 관측에서 추출) |
| 거리 | 픽셀 blur L2 | **곡선 대 곡선** (굵기가 지표에 안 들어감) |
| 해상도 | 28×28 (MNIST 유산) | **256×256 작업 / 출력은 별도** |
| LLM 역할 | 연속 파라미터 조종 | **구조(획 구성) 제안** |
| VLM 역할 | 매 라운드 비평 | **순위 감사 (심판이 아니라 제보자)** |
| 변동 | 전역 파라미터 섭동만 | **+ 목표점 공분산 섭동** |

### 왜 자모 단위인가 — "한 장" 이 "샘플 400개" 가 된다

한 장에서 평균 필체는 뽑히지만 **변동 구조**는 못 뽑는다 — 통글자로 보면 그렇다.
자모로 내려가면 아니다. 참조 사진(한글 ~130자) 눈대중 추정:

```
통글자 '미'   4회        ← 변동을 잴 수 없음
자모  ㅇ  ~40-50회
      ㅣ  ~35회
      ㅏ  ~30회
      ㅁ  ~15회        ← 평균·분산·공분산까지 측정 가능
      ㅂ  ~12회
```

흔한 자모는 10~50회 반복된다. 드문 자모(ㅋㅌㅍㄸ 등)만 공용 사전분포로 때운다.
duplicated-signature 문헌이 피험자당 1장에서 작동하는 구조가 이것이다.

### 변동은 세 층이다

| 층 | 무엇이 흔들리나 | v3 | v4 |
|---|---|---|---|
| 전역 | 글자 전체가 기울고 커짐 | ✅ `jitter_style` | 유지 |
| **골격** | ㄱ 의 꺾임 각도가 매번 다름 | ❌ | **목표점 공분산 섭동** |
| 이체 | 어떤 날은 열린 4, 어떤 날은 닫힌 4 | △ 가중치 추첨 | 유지 |

**"'가' 끼리 티가 난다" 는 정확히 골격 층이 없을 때의 증상이다.** 전역만 흔들면
같은 도장을 매번 다르게 찍는 것이지 다시 그리는 것이 아니다.
섭동은 **목표점 단위**로 한다 (`refer_paper/summary.md` §1.5 — 동역학이 아니라
기하를 건드리므로 가독성 보존에 유리).

### 조립 — 획 → 자모 → 음절 → 줄

획으로 내려가면 조립이 **새로 생기는 문제**다. v3 는 통글자 좌표를 손으로 박아둬서
조립이 공짜였다. 층마다 답이 다르다.

| 층 | 누가 | 재료 |
|---|---|---|
| 획 → 자모 | 아무도 안 만듦 — **관측에서 통으로 옴** | 그 사람 필기 |
| 자모 → 음절 | **표준 배치 앵커 + 관측된 어긋남** | 조합형 폰트 + 그 사람 필기 |
| 음절 → 줄 | **측정** (자간·베이스라인) | 사진에서 직접 잼 |

저장·재사용 단위는 **자모**, 표현 단위는 **획**. ㄹ 은 자모 하나지만 획은 셋이고,
개인차는 획 안에 있으므로 둘 다 필요하다.

**새 실패 모드가 생긴다** — 획은 하나하나 잘 맞는데 조립이 어긋나 글자로 안 읽히는 상태.
v3 에서는 불가능했던 실패다. 숫자는 자모 재사용이 없어 이 문제가 거의 없으므로,
검증은 숫자에서 먼저 한다.

### 루프 — 두 겹

```
   [LLM] 구조 가설 N개 제안 ◀────────────────────────────┐
        │   "이 사람 ㅁ 은 3획, 좌상단 시작, 우하단 겹침"    │
        │   이산·조합·위상. 수치 최적화가 원리적으로 못 품    │
        │                                              │
   후보마다 파라미터 피팅 (수치, 결정적)                    │
        │   → 잔차 r₁..rₙ                               │
        │                                              │
   코드가 순위 매김 → 잠정 승자                            │
        │                                              │
        ├─ 트리거 안 걸림 ──────────────────────→ 확정    │
        │                                              │
        └─ 트리거 걸림                                   │
             │                                         │
        [VLM] 패널 1장 (타겟 + 후보 N개, 블라인드·셔플)     │
             "이 중 타겟과 제일 닮은 건? 왜?"              │
             │                                         │
             ├─ 코드 1위와 일치 → 확정. 지표 신뢰 +1       │
             │                                         │
             └─ 불일치                                  │
                  ├─ 선호쌍 (A ≻ B) 로그 적재             │
                  ├─ 불일치율 갱신                        │
                  └─ VLM 서술 + 잔차 붙여 재제안 ─────────┘

   ──────────────────────────────────────────────────────
   바깥 루프 (느림): 선호쌍 수십 개 → 지표의 어느 항이 뒤집는지
                    진단 → 목적함수 수리 → 안쪽 루프 전체가 좋아짐
```

**역할 분담**

| 무엇 | 성질 | 누가 |
|---|---|---|
| 구조(획 구성·순서·위상) 고르기 | 이산·조합적 | **LLM** |
| 파라미터 맞추기 | 연속. σ 는 닫힌 해 있음 (§1.4) | 수치 최적화 |
| 궤적 구하기 | **닫힌 형태** (§1.2). 적분 불필요 | 그냥 계산 |
| 순위 매기기 | — | 코드 (잔차) |
| 순위 감사 | 상대·이산 판단 | VLM |
| 목적함수 수리 | — | 사람 (당분간) |

**VLM 은 심판이 아니라 제보자다.** 승자는 항상 코드가 정한다. VLM 은 깃발만 올리고,
그 깃발은 파라미터가 아니라 **지표를 고칠 신호**다. 그래야 아래층 수치 최적화가
그대로 돈다.

**감사 트리거** (매번 부르지 않는다)

| 조건 | 이유 |
|---|---|
| 1·2위 잔차 차이 < ε | 뒤집힐 위험 구간 |
| **잔차가 전부 나쁨** | **여기는 반드시.** 없으면 무한루프 |
| 자모당 최소 1회 | 불일치율 표본 확보 |

> "전부 나쁘면 LLM 에 재요청" 은 감사자 없이는 무한루프다. 원인이 *구조가 틀렸다* 인지
> *지표가 고장났다* 인지 코드는 구분할 수 없고, 후자면 새 구조를 아무리 뽑아도
> 잔차가 안 떨어진다.

**비용**: 후보를 패널 1장으로 묶으면 호출 수가 후보 수와 무관하다
(`loop.py::_panel` 이 이미 함). 자모 24 × 1~2라운드 = 필기자당 **20~50콜**.
필기자 등록 시 1회 도는 오프라인 작업이고, 생성 시점에는 VLM 이 돌지 않는다.

**감사 프롬프트 필수 조건**: 잔차를 알려주지 말 것(자기충족이 됨), 후보 순서를 매번
섞을 것(위치 편향), 어느 것이 코드 1위인지 표시하지 말 것.

### 해상도

28×28 은 MNIST(평가 대상)에서 온 것이지 모델링에서 온 것이 아니다. 셋을 막고 있었다.

1. **자모 분리 불가** — '방' 을 28×28 로 만들면 ㅂ/ㅏ/ㅇ 이 각 ~10×14px. 세선화 안 됨
2. **VLM 감사 불가** — `_panel(scale=8)` 로 확대해 보여주고 있던 것 자체가 증거
3. **픽셀 비교에 갇힘** — 세선화가 안 되니 곡선을 못 뽑고, 곡선이 없으니 픽셀로 비교할
   수밖에 없고, 그래서 굵기가 지표에 섞였다

| | 크기 | 근거 |
|---|---|---|
| 작업 (분해·피팅·감사) | **256×256** | 원본 사진이 글자당 150~250px 을 준다. 업/다운샘플 거의 없음 |
| 출력 (인식기 학습 데이터) | 인식기 요구대로 | 마지막에 다운샘플. 역방향은 불가능 |

자모당 실효 해상도 ~100×150, 획 굵기 8~15px — 세선화 안정 구간.
피팅이 곡선 공간에서 돌면 픽셀 수와 무관하므로 속도 문제 없다.

> 해상도가 목적함수 결함을 *없애지는* 않는다. 굵기 부풀리기 유인은 기하학적이다.
> 다만 해상도가 곡선 비교로 가는 길을 막고 있었고, **곡선 비교가 근본 해결이다.**

---

## 질문 순서

### Q1. 한 장에서 자모가 분리되는가? ★ 지금

**설계 전체의 밑돌. 안 되면 나머지가 전부 무의미하다.** 반나절이면 답이 나온다.

참조 사진에서 같은 자모(ㅁ 또는 ㅇ)를 전부 잘라내 정렬하고 겹쳐 본다.

- 성공: 자모 인스턴스 10개 이상이 깨끗이 분리됨
- 동시에 얻는 것: **자모 빈도 실측** (위 추정이 눈대중이므로), **변동이 어떻게 생겼는지**
  (어디가 흔들리고 어디가 고정인지 → 골격 층 섭동의 사양이 여기서 직접 나옴)
- 실패해도 수확: 어디서 막히는지 (연서? 해상도? 받침 붙음?)
- VLM·ODE·최적화 전부 불필요. 조명 평탄화 + 연결성분 + 자모 분리 + 겹치기

### Q2. 획 추출과 곡선 피팅이 그 사람 골격에 도달하는가?

v3 가 못 맞춘 것들로 검증한다 — 실제 `7`(0.73), `0`(0.57), 사장님 `5`·`6`.
세선화 → 폴리라인 → 원호 3~5개 근사 → 곡선거리.
이체 메뉴 최선값과 비교. **v3 의 실패 1을 직접 친다.**

### Q3. 곡선 거리가 시각 유사도와 같은 방향으로 움직이는가?

**심판 검증. v3 가 건너뛴 단계다.**
굵기를 바꿔가며 곡선거리가 반응하지 않는지 확인(반응하면 설계가 틀린 것).
골격을 일부러 틀어가며 거리가 단조로운지 확인.

### Q4. 구조 탐색 루프가 도는가? 그리고 **불일치율은 얼마인가?**

지금 아는 것은 지표 고장 **사례 하나**뿐이다. 비율을 모른다.

```
불일치율 < 10%  → 지표 믿을 만함. 자동 진행
불일치율 > 30%  → 지표 고장. 생성 파이프라인 짓기 전에 목적함수부터
```

**이 숫자가 진로를 가르는데 지금 없다.** 감사자를 넣는 첫 번째 값어치는
승자 선택이 아니라 이 비율의 계측이다.

### Q5. 안 본 글자로 전이되는가? — 홀드아웃

그 사람 필기에서 특정 글자를 빼고 피팅한 뒤, 그 글자를 생성해 실제와 비교.
v3 에서는 안 본 글자를 기본 이체로 때워서 **비교 대상 자체가 없었다.**
자모 단위면 안 본 글자가 **채점 가능한 예측**이 된다.
조립(자모→음절)이 되는지에 대한 유일한 정직한 시험.

### Q6. 이 데이터가 대안보다 나은가?

여기서 **처음으로** 인식 정확도 실험이 의미를 가진다.
비교: 폰트 렌더 / 기존 도구(synthtiger 등) / 실제 데이터 소량 / 합성+실제 혼합.

> 기록: 시뮬레이터 81.4% vs 폰트 86.6% vs 실제 99.5% (MNIST, 20k, 동일 조건).
> 당시 글자당 형태가 사실상 하나뿐이었다. 이후 이체를 34개로 늘렸으나 재측정 안 함.
> v4 는 구조가 달라 이 수치를 승계하지 않는다.

### Q7. 확장

한글 조판 앵커 / 금융 필드 문법 / constrained decoding + `illegible` 분기 + reject / 배포.

---

## 보류

- **ΣΛ 속도 모델** — 정지 이미지에는 속도가 없다 (`summary.md` §1.6, "계획에 결정적인 제약").
  당분간 **기하만** 쓴다: action plan(가상 목표점 + 원호)만 정지 이미지에서 복원.
  운동학적 사실감은 포기하되 골격 연속화는 얻는다. 나중에 태블릿 데이터가 생기면
  그 위에 lognormal 속도를 얹으면 되고, 구조가 호환된다.
- **확산 모델 하이브리드** — 시뮬레이터가 궤적·라벨을 만들고(정답 통제),
  확산 모델은 image-to-image 로 렌더 사실감만 담당(종이 질감·잉크 번짐).
  Q6 에서 사실감이 병목으로 나오면 이 순서로.
- **자동 목적함수 수리** — 선호쌍 로그가 충분히 쌓이고 패턴이 뚜렷해진 다음.
  당분간 사람이 로그를 본다 (`4a00c05` 에서 손으로 한 작업이 그것).

---

## 왜 GAN/확산이 아닌가

폰트 생성형(zi2zi, MX-Font, FS-Font 류)은 **출력이 폰트라 정의상 글자당 형태가 하나**다.
변동이 0 이라 즉시 티가 난다.

손글씨 **이미지** 생성 쪽(GANwriting, HWT/VATr, WordStylist, One-DM 등)은 변동을 내긴 한다.
그럼에도 이쪽을 택하는 이유 셋:

1. **한글** — 그쪽 문헌은 거의 영어/라틴(IAM 기반). 한글은 11,172자 조합 구조
2. **소량** — 우리 전제가 "라벨 부족". 학습이 필요한 방법은 전제와 충돌
3. **라벨 정확성** — 인식기 학습 데이터를 만드는 것이므로 라벨이 틀리면 안 된다.
   생성 모델은 가끔 다른 글자를 그린다. `summary.md` §1.6:
   *"정답을 우리가 완전히 통제한다 — 학습 데이터 생성에 정확히 필요한 성질"*

```
              파라미터 수        필요 데이터
확산 모델      수억~수십억        수천 장
ΣΛ/기하        ~400개            1장
              (자모24 × 획2.5 × 6)
```

파라미터는 백만 배 차이인데 **표본 효율은 반대다.** 방정식이 사전지식을 공짜로 넣어주기
때문이고, 그 사전지식이 §1.3 의 스타일/내용 분해다 — 확산 모델은 이 분리를 데이터로
배워야 하고 우리는 물리에서 받아온다. **"과거로 돌아가기" 가 아니라 "배울 것과 아는 것을
나누기" 이고, 데이터가 한 장일 때는 그것 말고 되는 방법이 없다.**

---

## 현재 자산

| | 상태 |
|---|---|
| `phase0/extract.py` | **재사용.** 조명 평탄화·연결성분·deskew. 크롭 크기 파라미터화 필요 |
| `phase0/simulator.py` | **부분 재사용.** 렌더링·스타일 파라미터는 살아남음. `ALLOGRAPHS`(이체 34개)는 Q2 결과에 따라 폐기 후보. `to_mnist_format` 크기 파라미터화. `stroke_w` 범위는 28px 안티앨리어싱 영향이 있어 **재측정 필요** |
| `phase0/loop.py` | **부분 재사용.** `_panel()` 은 감사 패널로 그대로. `blur_l2` 는 곡선거리로 대체 (과도기엔 sigma 를 해상도에 비례 조정) |
| `phase0/contract.py` | **재작성.** 프롬프트가 전역 파라미터 조종용. 구조 제안 + 순위 감사용으로 새로 |
| `phase0/writer.py` | **부분 재사용.** `jitter_style` 은 전역 층에 그대로. `fit_writer` 는 곡선 피팅으로 대체 |
| `phase0/reach.py` | 재사용 (도달 가능성 측정 개념 동일) |
| `phase0/data.py`, `oracle.py`, `run.py`, `analyze.py` | **28×28 MNIST 전용. Q6 까지 보류** |
| `refer_paper/summary.md` | 검증 등급 표기된 리서치. §1.2/§1.3/§1.4/§1.5/§1.6 이 v4 의 근거 |

**신규 필요**: `jamo.py`(자모 분리), `trace.py`(세선화 → 폴리라인 → 원호 근사),
`curvedist.py`(곡선 거리), `audit.py`(감사 패널 + 선호쌍 로그)

**의존성**: `scikit-image` 추가 필요 (세선화). 현재 컨테이너에 `scipy` 도 미설치.

---

## 미결

1. 대상 문자·필드 — 숫자만? 한글 이름? 금액 한글표기?
2. 397B 가 이미지 입력을 받는가 — 아니면 별도 VLM 필요
3. **태블릿 데이터를 받을 수 있는가** — 받으면 ΣΛ 속도 모델이 열리고 보류 항목이 풀린다.
   문헌은 피험자당 1장에서 작동. 코드 문제가 아니라 협조 문제
4. 조판 앵커를 어디서 뽑는가 — 조합형/자모 단위 폰트는 자모 경계가 분리돼 있으나,
   완성형 폰트는 통글자라 자동 추출 불가. **실물 확인 안 함**
5. 연서(붙여 쓴 글씨) — 자모 경계가 이미지에 없으면 분해도 조립도 무의미.
   금융 양식 숫자·금액은 유리하나 **이름 필드는 흘려 쓸 수 있다.** Q1 에서 같이 본다
6. 배포 제약 — 내부망, 모델 크기, 레이턴시
7. LLM 기반 방정식·구조 탐색 선행연구(FunSearch, OPRO, SINDy/PySR 계열)는
   **`[미확인]`** — 기억 인용이다. 설계에 본격 반영 전 `summary.md` §7 로 등급 표기해 정리

> 내부 금융 양식·필기 샘플을 외부에 업로드하기 전 부서장과 협의.
