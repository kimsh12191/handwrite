# 금융권 손글씨 인식 프로젝트 — 계획 (v2)

> v1 은 폐기. 사유는 문서 맨 뒤 [부록 A] 참조.

---

## 1. 목표

**최종**: 금융권 양식의 손글씨를 인식하는 모델. 필드 타입이 정해져 있다는 **prior** 를 적극 활용한다 (금액 필드는 숫자만, 이름 필드는 제한된 사전 등).

**경로**: 실제 라벨 데이터가 부족하므로 **합성 데이터를 만들어서** 학습한다. 단 폰트 기반이 아니라, **손글씨를 그리는 시뮬레이터**를 만들고 그 파라미터를 조절해 생성한다.

**핵심 아이디어**: 개인의 필체를 **해석 가능한 파라미터 벡터**로 표현한다. 그 파라미터를 고정한 채 텍스트만 바꾸면, **같은 사람이 쓴 다른 글자**가 나온다.

---

## 2. 핵심 구조

리서치 결과, 이 아이디어는 Plamondon 의 **Kinematic Theory / Sigma-Lognormal (ΣΛ)** 모델과 정확히 일치한다 (1995~, 40년간 검증). 자의적 설계 대신 이 이론을 채택한다. 상세 수식은 `refer_paper/summary.md` §1.

**결정적인 것은 "내용"과 "스타일"의 분리다.** ΣΛ 는 이 분리를 이론적으로 정당화해준다:

```
내용 (무엇을 쓰는가)                     스타일 (어떻게 쓰는가)
= action plan                          = 그 사람의 운동 프로그램
= 획별 (D, θs, θe)                      = 신경근 파라미터 (μ, σ)
= 숫자/자모 스켈레톤 DB (고정)             + 획 겹침 (t0 분포)
                                       + 기하 (slant, 크기, 곡률, tremor...)
                                       + 렌더링 (펜촉, 잉크, 필압)
            \                                  /
             \                                /
              ΣΛ 합성 → 궤적 → 잉크 렌더링 → 이미지
```

| 파라미터 | 의미 | 분류 |
|---|---|---|
| $D_i$ | 획의 호 길이 | 내용 |
| $\theta_{si}, \theta_{ei}$ | 원호 시작/끝 방향 (차 = 곡률) | 내용 |
| $t_{0i}$ | 운동 명령 발화 시각 → **획 겹침** | 타이밍 |
| $\mu_i$ | 신경근 응답 지연의 로그 (쓰기 속도) | **스타일, 개인 고유** |
| $\sigma_i$ | 속도 프로파일의 왜도 (매끄러움/피로/연령) | **스타일, 개인 고유** |

획들이 **시간축에서 겹치는 정도**가 이산적 원호를 매끄러운 흘림체로 바꾼다. 가장 표현력 있는 스타일 노브 중 하나.

**한글**: 11,172 음절 → **기본 자모 24개** (복합 포함 51, 초/중/종성 구분 시 68). 자모 단위로 획을 정의하면 전체를 커버한다. *(검증 완료)* 라틴 흘림체보다 우리 접근에 훨씬 유리하며, 리서치 결과 **이걸 한 발표 연구가 없다.**

---

## 3. 반드시 알아야 할 제약

### 3.1 ΣΛ 는 이미지에서 파라미터를 뽑을 수 없다

ΣΛ 는 **속도 모델**이다. 스캔 이미지에는 시간축이 없으므로 $\mu, \sigma, t_0$ 는 원리적으로 복원 불가능하다.

**이것이 "이미지 → LLM → 파라미터 역추론" 구상을 수정하게 만든다.** 하지만 치명적이지는 않다. 우리의 실제 목표는 *그 사람의 운동 프로그램을 알아내는 것*이 아니라 **렌더링 결과가 타겟처럼 보이게 만드는 것**이기 때문이다.

- **이미지에서 복원 가능**: slant, x-height, 획 굵기, 곡률, 자간, tremor 진폭, 연결성, 잉크 농도 → **역문제의 주 타겟**
- **이미지에서 복원 불가**: $\mu, \sigma$ 절대값, 획 순서, 실제 타이밍 → 모집단 분포에서 **샘플링**하거나, 외형에 미치는 간접 효과로만 약하게 추정

권장 우회로 (Ferrer et al. ICCST 2013): **온라인으로 생성 → 잉크 침착 모델로 오프라인 렌더링.** 정답을 우리가 완전히 통제하므로 학습 데이터 생성에 정확히 맞는 성질이다.

$\mu,\sigma$ 의 현실적 분포가 필요하면 **펜 태블릿으로 소량 수집**하는 것이 가장 확실하다. ΣΛ 의 강점이 극소량 샘플로 동작한다는 것이다 — duplicated-signature 문헌은 **피험자당 실제 1장**으로 작동한다.

### 3.2 LLM 단독 최적화는 최선이 아니다

"LLM 이 시뮬레이터를 굴린다"는 구상은 유지하되, 역할을 나눈다:

- **직접 측정으로 끝나는 것** (slant, 획 굵기, x-height, 자간): 이미지에서 **계산**한다. LLM 에게 묻지 않는다. 훨씬 정확하고 공짜다.
- **LLM 이 잘하는 것**: 초기값 제안, 의미적 판단("이건 흘려 쓴 글씨체"), 측정으로 안 잡히는 것(획 순서, 전반적 인상), 막혔을 때 방향 전환
- **수치 최적화가 잘하는 것**: 연속 파라미터 30여 개의 미세 조정 (CMA-ES / Nelder-Mead, 또는 렌더러를 미분 가능하게 만들면 gradient)

**VLM 에게 정성 서술만 받으면 수렴하지 않을 가능성이 높다.** "비슷한데 좀 다르네요"로는 파라미터를 못 고친다. **수치 델타를 함께 준다**: "목표 기울기 12°, 현재 21°, −9° 필요". VLM 은 측정으로 안 잡히는 부분만 보조한다.

### 3.3 합성 데이터의 가장 흔한 실패

스타일 파라미터를 **문서 단위가 아니라 문자 인스턴스마다** 재추첨해야 한다 (인접 문자가 상관되도록 Perlin 노이즈 권장). 안 그러면 페이지의 모든 "5"가 똑같아지고, 이것이 합성 손글씨의 **가장 명백한 티**다.

---

## 4. 단계별 계획

**원칙: 프로젝트를 죽일 수 있는 것부터 검증한다.** 완벽한 시뮬레이터를 먼저 만들고 나중에 안 되는 걸 발견하는 순서를 피한다.

### Phase 0 — 관문 실험 (며칠) ★ 최우선

**질문: 합성 손글씨로 학습한 모델이 진짜 손글씨에서 작동하는가?**

이게 프로젝트 전체의 전제다. 그런데 **정교한 시뮬레이터 없이 지금 당장 측정할 수 있다.**

1. 이미 검증된 스켈레톤+스타일 PoC (139줄, 숫자 렌더링 확인됨) 로 숫자 합성 데이터 ~50k 생성
2. 작은 CNN 학습
3. **MNIST 테스트셋으로 평가** — 실제 사람 손글씨 숫자, 무료, 즉시 사용 가능

**판정 기준**
- 정확도가 낮다 (~40% 미만) → 사실감이 근본 병목. 렌더링/도메인 갭에 투자를 몰아야 함
- 중간 (60–85%) → 접근 유효. 어느 요소가 갭을 만드는지 ablation
- 높다 (90%+) → 전제 확인. 계획대로 진행

**얻는 것**: 몇 주가 아니라 며칠 만에 프로젝트 성패 신호. 이후 모든 개선의 **기준선**.

> 여기에 더해, 반대 방향 기준선도 싸게 얻는다: 폰트 기반 생성 (TRDG, MIT) 으로 같은 실험. **시뮬레이터가 폰트보다 나은가**를 숫자로 답할 수 있어야 한다. 아니라면 시뮬레이터 자체가 정당화되지 않는다.

### Phase 1 — 시뮬레이터 (2–3주)

Phase 0 결과에 따라 우선순위 조정.

**1a. 스켈레톤 DB**
- 숫자 0–9 (획 1–3개) — 반나절
- 기본 자모 24개 — 이후
- 획 **순서와 방향** 포함 (사람처럼 보이게 하는 핵심)
- 정규화 em-box 상의 제어점 + 펜 up/down

**1b. ΣΛ 합성 엔진** (~수백 줄 NumPy)
- `refer_paper/summary.md` §1.2 의 닫힌 형태 사용 — 획별 수치적분 불필요
- 스켈레톤 제어점 → action plan (가상 목표점) → $(D,\theta_s,\theta_e)$
- $(\mu,\sigma,t_0)$ 로 속도 프로파일 생성, 시간 겹침으로 획 합성
- 참조: `andrew-healey/sigma-lognormal` (수식 정확성 검증됨, 단 취미 수준·무라이선스 — **읽기용으로만**)

**1c. 렌더러** — 도메인 갭의 핵심, 과소평가 금물
- 속도 → 필압 → 가변 폭 브러시
- 잉크 침착 모델 (볼펜/만년필), 번짐, 끊김
- 종이 질감, 스캔 노이즈, JPEG 아티팩트, 미세 회전, 조명 불균일
- 금융 양식 특성: 괘선·격자 위에 쓰기, 칸 넘침, 인쇄 텍스트와 겹침

**1d. 스타일 파라미터 세트 확정** (~30–40개)
- 각 파라미터에 이름, 물리 단위, 타당 범위, 인스턴스별 지터 폭을 명시
- **LLM 이 읽고 조작할 수 있는 형태로 문서화** (JSON 스키마 + 설명)

### Phase 2 — 역문제 (2–3주)

**2a. 이미지 측정기** (LLM 불필요)
slant (획 방향 PCA/Hough), 획 굵기 (거리 변환), x-height·baseline (수평 투영), 잉크 밀도, 곡률 히스토그램, 자간, 연결 성분 통계

**2b. self-inversion 검증** ★ 이 단계에서 가장 중요
1. 랜덤 $\theta_{true}$ 샘플 → 이미지 생성
2. 역문제 루프를 그 이미지에 실행
3. $\|\theta_{recovered}-\theta_{true}\|$ 와 이미지 유사도 측정

**정답을 아는 상태에서 정량 평가가 가능하다.** 여기서 안 되면 실제 이미지에서는 절대 안 된다. 비용이 거의 0이므로 실제 데이터를 건드리기 전에 반드시 통과시킨다. 어느 파라미터가 식별 가능/불가능한지도 여기서 드러난다.

**2c. LLM 루프**
```
타겟 이미지
  → [측정기] 수치 통계
  → [LLM] 초기 파라미터 제안 (JSON)
  → 반복:
       시뮬 → 렌더 → 비교(수치 메트릭 + VLM 서술)
       → [LLM] 파라미터 수정
     수렴 또는 예산 소진까지
```
**2d. 하이브리드**: LLM 이 거친 탐색·의미 판단, 수치 최적화가 미세 조정.

### Phase 3 — 인식 모델 + Prior (3–4주)

**3a. 데이터 생성 파이프라인** — 필드 타입별, 스타일 다양성 통제

**3b. 소형 VLM 파인튜닝** — 내부망 배포 제약 확인 필요

**3c. Prior 주입** — 프롬프트만으로는 불충분. **constrained decoding** 이 본체:
- 필드 문법을 DFA/regex 로 (금액 `^[0-9]{1,3}(,[0-9]{3})*$`, 계좌번호 패턴, 날짜 범위)
- 이름·지점명은 어휘 prefix tree
- 백엔드: Outlines / XGrammar / llguidance (vLLM·SGLang 지원)
- DFA 는 선형 시간 → 추론 비용 사실상 0

> ⚠️ **필수 안전장치**: 문법에 **명시적 `illegible` 분기**를 넣는다. 형식을 강제하면 정직한 "못 읽겠다"가 **자신 있게 형식만 맞는 오답**으로 바뀐다 — 은행 응용에서 실제 위험이다. logprob 을 신뢰도로 삼아 reject 옵션에 연결한다.

**3d. 교차검증 (CAR/LAR)** — 금액을 숫자와 한글로 둘 다 적는 필드가 있다면, 두 채널의 오류가 **통계적으로 독립**이므로 일치 여부가 매우 강한 증거가 된다. 1997년 문헌 보고: 1% 오류율에서 ~67% 인식. **불일치 → 사람에게 라우팅.** 시스템의 가치는 **모를 때 모른다고 아는 것**에 있다.

**3e. 평가** — 실제 데이터 홀드아웃 필수. ablation: prior on/off, 합성만 vs 합성+실제, 스타일 다양성 vs 정확도

### Phase 4 — 한글 확장

자모 24개 스켈레톤 → 초/중/종성 위치별 변형 → 음절 조립. 숫자에서 파이프라인이 검증된 후 진행.

---

## 5. 리스크

| # | 리스크 | 검증 시점 | 완화 |
|---|---|---|---|
| R1 | 합성→실제 도메인 갭이 커서 인식 성능이 안 나옴 | **Phase 0** | 렌더러 사실감에 투자, 소량 실제 데이터 혼합 |
| R2 | 역문제 루프가 수렴 안 함 | **Phase 2b** | 정답 아는 self-inversion 으로 선검증, 수치 최적화 병행 |
| R3 | VLM 피드백이 너무 모호해 신호가 안 됨 | Phase 2c | 수치 메트릭을 주 신호로, VLM 은 보조 |
| R4 | 실제 손글씨 샘플이 없어 검증 자체가 불가 | **지금** | MNIST 로 시작, 내부 데이터 확보 협의 |
| R5 | 시뮬레이터가 폰트 기반보다 낫다는 근거 없음 | **Phase 0** | 폰트 기준선을 같이 측정 |
| R6 | constrained decoding 이 오답을 자신 있게 만듦 | Phase 3c | `illegible` 분기 + reject 옵션 |

---

## 6. 결정이 필요한 사항

계획을 더 구체화하려면 답이 필요하지만, **Phase 0 은 답 없이도 시작 가능**하다.

1. **대상 문자·필드** — 숫자만? 한글 이름? 금액 한글표기? 영문? → 작업량이 수 배 차이
2. **397B 모델이 이미지 입력을 받는가** — 텍스트 전용이면 별도 VLM 필요, 루프 설계가 달라짐
3. **실제 손글씨 샘플 확보 가능한가** — 없으면 최종 검증 불가. 펜 태블릿 소량 수집이 가능하면 ΣΛ 파라미터 분포의 실측 근거가 생김
4. **인식 모델 배포 제약** — 내부망, 모델 크기, 레이턴시, GPU

> **컴플라이언스**: 내부 금융 양식 데이터나 실제 필기 샘플을 외부 서비스에 업로드하기 전 부서장과 협의할 것.

---

## 7. 문서 원칙

v1 에서 실행된 적 없는 622줄 코드에 826줄 문서를 썼다. 반복하지 않는다.

- **작동을 확인한 것만 문서화한다**
- 코드를 쓰면 **먼저 돌려본다**
- 계획서(이 문서) + 검증된 리서치(`refer_paper/`) 외의 문서는 해당 Phase 가 실제로 작동한 뒤에 쓴다

---

## 부록 A — v1 폐기 사유

v1 의 `simulator/` 는 **글자를 그릴 수 없었다.** 실측으로 확인:

```
v̇x = -α·vx - β·x + γ·sin(ωt) + c·text_feat
v̇y = -α·vy - β·y + γ·sin(ωt) + c·text_feat    ← x 식과 완전히 동일
```

- $x(t)$ 와 $y(t)$ 의 상관계수 **정확히 1.000000**, $\max|x-y| = 0.0$
- → 궤적이 $y=x$ 직선에 갇힘. 어떤 텍스트를 넣어도 **대각선 하나**
- 방향 전환 0회 (글자는 다수의 방향 전환을 요구)

**근본 원인**: 감쇠 진동자는 *어떻게* 움직이는지만 기술한다. **"무엇을 쓰는가"라는 정보가 모델 어디에도 없었다.** 텍스트는 스칼라 상수 하나로 들어가 직선 길이만 바꿨다.

**절차 실패**: 한 번도 실행하지 않고 문서를 작성했다. `test_phase1.py` 를 돌렸다면 즉시 발견했을 문제다.

**Neural ODE 계획도 폐기**: 동역학을 신경망으로 학습시키면 파라미터가 블랙박스가 되어 **LLM 이 조작할 대상이 사라진다.** 학습 없이 LLM 이 시뮬레이터를 굴린다는 원래 컨셉과 모순이다.

**v1 리서치도 폐기**: 저자가 대부분 "Not specified" 였고 링크를 검증하지 않았다. 무엇보다 **ΣΛ 를 부록으로 밀어놨다** — 이 프로젝트의 핵심 이론인데.

복구가 필요하면 커밋 `6edb3d1` 에 남아 있다.
