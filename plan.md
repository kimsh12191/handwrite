# 금융권 손글씨 인식 프로젝트 — 계획 (v3)

## 목표

금융권 양식의 손글씨를 인식한다. 실제 라벨 데이터가 부족하므로 **합성 데이터로 학습**한다.
폰트가 아니라 **시뮬레이터**를 쓰는 이유는 특정 필체에 **맞출 수 있기** 때문이다.

```
타겟 손글씨 이미지 → LLM 이 시뮬레이터 파라미터를 고름 → 그 파라미터로 임의 텍스트 생성
```

---

## 원칙

**컴포넌트를 쌓는 순서가 아니라, 답이 나오면 다음 결정이 바뀌는 질문 순서로 진행한다.**

v1·v2 에서 두 번 실패한 원인이 같다 — 핵심 질문 주변에 인프라를 짓고 핵심 질문은 건드리지 않았다.
- v1: 시뮬레이터를 한 번도 실행하지 않고 문서부터 씀 (글자를 못 그리는 코드였음)
- v2: 합성 데이터로 분류기를 학습해 MNIST 정확도를 잼 (분포 커버리지 질문, 원래 Q4)
- v2': 파라미터 스윕 (내가 짠 코드가 짠 대로 도는지 확인. 구조적으로 참)

---

## 질문 순서

### Q1. LLM 이 시뮬레이터를 조종해 타겟 필체를 맞출 수 있는가? ★ 지금

프로젝트의 성패가 걸린 유일한 질문. 인프라 없이 지금 답할 수 있다 — **내가 LLM 이므로 직접 루프를 돈다.**

```
타겟 이미지 본다 → 파라미터 제안 → 렌더 → 나란히 본다 → 수정 → 반복
```

- 타겟: MNIST 숫자 몇 장 (실제 사람 손글씨)
- 성공: 시각적으로 근접
- 실패해도 수확: 어디서 막히는지 드러남
  (차이를 못 보는가 / 봐도 파라미터로 못 옮기는가 / 파라미터 공간이 도달 못 하는가)
- VLM·397B 불필요. 자동화는 Q3.

### Q2. 맞춘 파라미터가 다른 글자로 전이되는가?

`4` 를 맞춘 파라미터로 `0~9` 를 쓰면 **같은 사람이 쓴 것처럼 보이는가.**
이 프로젝트의 핵심 주장이다. 안 되면 전제가 무너진다.

### Q3. 그 루프가 자동화되는가?

수동 루프를 397B + 이미지 측정치로 대체.
- 측정 가능한 것(기울기·굵기·크기·자간)은 **계산**한다. LLM 에게 묻지 않는다.
- LLM 은 초기값, 이체 선택, 의미 판단, 막혔을 때 방향 전환.
- 연속 파라미터 미세조정은 수치 최적화.
- Q1 에서 **내게 실제로 필요했던 피드백**이 무엇인지가 설계 근거가 된다.

### Q4. 이 데이터가 대안보다 나은가?

여기서 **처음으로** 인식 정확도 실험이 의미를 가진다.
비교: 폰트 렌더 / 기존 도구(synthtiger 등) / 실제 데이터 소량 / 합성+실제 혼합.

> 기록: 시뮬레이터 81.4% vs 폰트 86.6% vs 실제 99.5% (MNIST, 20k, 동일 조건).
> 당시 시뮬레이터는 글자당 형태가 사실상 하나뿐이었다. 이후 이체를 34개로 늘렸으나 재측정 안 함.
> 이 실험은 Q1~Q3 이후에 다시 한다.

### Q5. 확장

한글(자모 24개 단위) / 금융 필드 문법 / constrained decoding + `illegible` 분기 + reject / 배포.

---

## 보류

- **ΣΛ (Sigma-Lognormal)** — 속도 프로파일 모델이라 *운동학적* 사실감을 준다.
  Q1 은 정지 이미지를 맞추는 문제라 기하 파라미터가 지배적이다.
  Q4 에서 사실감이 병목으로 확인되면 그때 넣는다. 수식은 `refer_paper/summary.md` §1 에 정리됨.

---

## 현재 자산

| | |
|---|---|
| `phase0/simulator.py` | 숫자 0-9, 이체 34개, 스타일 파라미터 13개, MNIST 형식 렌더 |
| `phase0/data.py` | MNIST 로더, 합성/폰트 생성기, 대조 시트 |
| `phase0/oracle.py` | 가독성 판정기 (MNIST 학습 분류기) |
| `phase0/run.py`, `analyze.py` | Q4 용. 지금은 쓰지 않음 |
| `refer_paper/summary.md` | 검증 등급 표기된 리서치 |

---

## 미결 (Q1 은 답 없이 진행 가능)

1. 대상 문자·필드 — 숫자만? 한글 이름? 금액 한글표기?
2. 397B 가 이미지 입력을 받는가 — 아니면 별도 VLM 필요
3. 실제 손글씨 샘플 확보 가능한가 — 없으면 최종 검증 불가
4. 배포 제약 — 내부망, 모델 크기, 레이턴시

> 내부 금융 양식·필기 샘플을 외부에 업로드하기 전 부서장과 협의.
