# 손글씨 인식 모델 프로젝트 계획

## 최종 목표
- 금융권에서 필요로 하는 손글씨 인식 모델
- Prior 정보 활용 (예: 특정 필드는 숫자만, 이름은 제한된 사전 등)

## 핵심 아이디어
**동역학 기반 손글씨 시뮬레이터 + LLM 역추론**

개인의 필체 특성을 미분방정식(ODE) 파라미터로 정의하고, 이를 통해 다양한 텍스트의 손글씨 이미지를 생성. 
LLM이 이 파라미터를 역추론함으로써 대규모 합성 데이터셋 생성.

---

## 프로젝트 구조

```
손글씨 이미지
    ↓
[LLM] ODE 파라미터 추론
    ↓
[시뮬레이터] 파라미터로 손글씨 생성
    ↓
[VLM] 원본 vs 생성본 비교 분석
    ↓
[LLM] 피드백으로 파라미터 수정
    ↓
반복 (수렴할 때까지)
```

### 핵심 특징
- **LLM In-Context Learning**: 파인튜닝 없이 chain-of-thought 추론
- **Auto-Research 루프**: VLM 피드백으로 자체 개선
- **파라미터 기반 생성**: 동역학 모델 → 텍스트 독립적 다양한 손글씨 생성

---

## 개발 단계

### Phase 1: 리서치 & 동역학 모델 선정
**목표**: 손글씨 동역학 모델링 기존 연구 조사, 최적 ODE 구조 선택

**작업**:
- 손글씨 동역학 모델링 관련 논문 수집
- 기존 ODE/상태공간 모델 분석
- 최신 접근 방식 검토
- 파라미터 차원수 결정
- 모델 비교 및 선정

**산출물**: `refer_paper/` 폴더 + `summary.md`

### Phase 2: 시뮬레이터 프로토타입
**목표**: 선정된 ODE 모델을 기반한 손글씨 생성 시뮬레이터 구현

**작업**:
- ODE 정의 및 수치해석 구현 (scipy.integrate)
- 파라미터로부터 펜 궤적 생성
- 이미지 렌더링 (Canvas/PIL)
- 테스트: 여러 파라미터 조합으로 다양한 필체 생성

**산출물**: `simulator/` 모듈

### Phase 3: 역추론 모듈 개발
**목표**: 이미지로부터 ODE 파라미터 역추론

**선택 1 - 클래식 방식**:
- 실제 손글씨 궤적 추출
- 궤적으로부터 ODE 파라미터 피팅

**선택 2 - LLM Auto-Research 방식** (권장):
```
원본 이미지 → LLM (파라미터 추론)
    ↓
시뮬레이터 (생성)
    ↓
VLM (비교)
    ↓
LLM (수정)
    ↓
반복
```

**산출물**: `inverse_inference/` 모듈

### Phase 4: 금융권 도메인 적용
**목표**: 특정 필드/문자 세트에 대한 손글씨 생성

**작업**:
- 대상 손글씨 유형 정의 (수표, 양식, 서명 등)
- Prior 정보 통합 (글자 제약, 사전 등)
- 데이터셋 생성 (100K+ 샘플)
- 인식 모델 학습 (small VLM fine-tuning)

**산출물**: `dataset_generator/`, `recognizer/`

---

## 기술 스택
- **LLM**: 397B 모델 (내부망 로컬 실행)
- **VLM**: Claude Vision 또는 유사 모델 (비교 분석)
- **시뮬레이터**: Python scipy, PIL/matplotlib
- **버전 관리**: Git

---

## 동역학 모델 (초안)

### 상태 벡터
$$\mathbf{s} = [x, y, \dot{x}, \dot{y}, p, \theta]^T$$
- $(x, y)$: 펜 위치
- $(\dot{x}, \dot{y})$: 필기 속도  
- $p$: 필압 (0~1)
- $\theta$: 펜 각도

### ODE 구조 (개인 파라미터 $\boldsymbol{\theta}$ 포함)

**위치 동역학** (damped nonlinear oscillator):
$$\ddot{x} = -\alpha \dot{x} - \beta x + \gamma \sin(\omega t + \phi) + \eta(c)$$

**필압 동역학**:
$$\dot{p} = -\lambda p + \mu(c) + \sigma v$$

**각도**:
$$\dot{\theta} = \kappa \frac{\dot{y}}{\dot{x}} + \rho(c)$$

### 파라미터 차원
목표: **20~30차원**
- 위치 동역학: 8개
- 필압: 4개
- 각도: 3개
- 문자별 modulation: 5~6개

---

## 타임라인
- **Phase 1 (리서치)**: 1-2주
- **Phase 2 (시뮬레이터)**: 2-3주
- **Phase 3 (역추론)**: 3-4주
- **Phase 4 (금융 적용)**: 4-6주

---

## 리스크 및 고려사항
1. ODE 파라미터의 수렴성: 역추론이 항상 유일해가 있는가?
2. LLM의 수치 정확성: 파라미터 벡터를 정확히 생성할 수 있는가?
3. 데이터 분포: 합성 데이터가 실제 손글씨 분포를 충분히 커버하는가?
4. 계산 비용: 대규모 데이터셋 생성 시 LLM 추론 비용
